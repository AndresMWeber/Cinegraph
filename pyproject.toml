[tool.poetry]
name = "cinegrade"
version = "0.1.0"
description = ""
authors = ["Andres Weber <andresmweber@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
opencv-python = "^4.5.3"

[tool.poetry.dev-dependencies]
black = "^21.7b0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = 'cinegrade.cli:run'
